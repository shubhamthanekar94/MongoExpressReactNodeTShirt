import React from "react";
import Base from "../core/Base";

//this page you can use for editing the logged in user's personal info such as contact number, Email, DOB
const Profile = () => {
  return (
    <Base title="Profile page">
      <h1>This is profile page</h1>
    </Base>
  );
};

export default Profile;
